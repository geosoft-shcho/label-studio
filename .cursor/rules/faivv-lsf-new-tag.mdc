---
description: Faivv LSF — 새 태그/컴포넌트 (상세 AGENTS는 agents-guide 서브에이전트)
globs: web/libs/editor/src/tags/**/*,web/libs/editor/src/components/MultimodalTimeline/**/*,web/libs/editor/src/components/SegmentAttachments/**/*,web/libs/editor/src/components/SavedSegmentAttachments/**/*
alwaysApply: false
---

# Faivv LSF — 새 태그 / 컴포넌트

구현·수정 전 **`agents-guide` 서브에이전트**에 위임한다 (`docs/AGENTS_FAIVV_NEW_TAG.md` · 폴더 `AGENTS.md`).

## 금지·주의 (요약)

- 저장 스키마 / faivv-flow `layer_persist` 무단 변경 금지
- XML `"true"`/`"false"` → MST `types.boolean`
- Control을 View children에 넣으려면 `tags/visual/View.jsx` union에 type 추가
- 빌드: `MODE=standalone npx nx run editor:build:production` 후 faivv-flow `web/tools/label-studio-v2/ls/` sync
